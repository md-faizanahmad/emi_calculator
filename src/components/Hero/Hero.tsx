import HeroClient from "./HeroClient";

export default function Hero() {
  return (
    <HeroClient
      title="EMI MITRA - SIMPLIFY YOUR LOAN CALCULATIONS"
      subtitle="Calculate EMIs for home, car, bike, product, or salary-based loans with ease and accuracy."
      ctaText="Try Our Calculators"
      ctaUrl="/"
    />
  );
}
